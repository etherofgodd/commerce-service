spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order-service
    username: root
    password: '#Mugshot23'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: ORDER-SERVICE
server:
  port: 8082